<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('http://loremflickr.com/1000/1000/movie');">
      <div class="container">
        <h1><%= @list.name %></h1>
        <p></p>
      </div>
    </div>

<div class="container flexbox">
<%= link_to 'back to all lists', lists_path, class: "btn btn-flat" %>
<%= link_to 'add a movie', new_list_bookmark_path(@list), class: "btn btn-flat"%>
</div>

<span>\</span>


<div class="container">
  <div class="cards">
    <% @movies.each do |movie| %>
      <div class="card-category" style='background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), <%= movie.poster_url %>'>
        <%= movie.title %>
      </div>
    <% end %>
  </div>
</div>


