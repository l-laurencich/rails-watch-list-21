<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('http://loremflickr.com/1000/1000/movie');">
      <div class="container">
        <h1><%= @list.name %></h1>
        <p></p>
      </div>
    </div>

<div class="container flexbox">
<%= link_to 'back to all lists', lists_path, class: "btn btn-flat" %>
<%= link_to 'add a movie', new_list_bookmark_path(@list), class: "btn btn-flat"%>
</div>

<span>\</span>


<div class="container">
  <div class="cards">
    <% @list.bookmarks.each do |bookmark| %>
      <div class="card-detail">
        <div class="card-category" style='background-image: url("<%= cl_image_tag bookmark.photo.key, height: 300, width: 400, crop: :fill %>")' >
        </div>
        <div class="card-detail-infos">
          <div class="flexbox bookmark-title-container">
            <h3><%= bookmark.movie.title %></h3>
            <p><%= bookmark.movie.rating %> <i class="far fa-star"></i></p>
          </div>
          <div class="overview">
            <p><%= bookmark.movie.overview %></p>
          </div>
          <div class="flexbox bottom-text">
            <div><p><i class="fas fa-quote-left"></i> <%= bookmark.comment %>     -</p></div>
            <div><p><%= link_to 'delete',  bookmark_path(bookmark), method: :delete %></p></div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>





